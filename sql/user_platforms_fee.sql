-- ค่าธรรมเนียม/ค่าคอมมิชชันของแพลตฟอร์ม
-- รันที่ Supabase SQL editor / idempotent: รันซ้ำได้ไม่พัง
--
-- ทำไมเก็บที่แพลตฟอร์ม ไม่ใช่ที่รายการลงทุน:
--   ค่าธรรมเนียมเป็นคุณสมบัติของ "ที่ที่เราซื้อ" ไม่ใช่ของ "ของที่เราซื้อ"
--   กรอกครั้งเดียวที่ Bitkub แล้วทุกไม้ที่ซื้อผ่าน Bitkub ใช้ตัวเลขเดียวกัน
--   (ยอดที่จ่ายจริงต่อดีลยังกรอกแยกได้ที่ realized_trades.fees เหมือนเดิม
--    ตัวเลขในตารางนี้เป็นค่าตั้งต้นไว้ประมาณการเท่านั้น)
--
-- ไม่ตั้ง (null) ต่างจาก 0: null = ยังไม่รู้ / 0 = ฟรีจริง ๆ
-- โค้ดฝั่งแอปแยกสองอย่างนี้อยู่ (estimatePlatformFee คืน null เมื่อยังไม่ตั้ง)
-- อย่าใส่ default 0 ให้คอลัมน์พวกนี้

alter table if exists public.user_platforms
  -- % ของมูลค่าที่ซื้อขายต่อ 1 คำสั่ง เช่น 0.25 = 0.25%
  add column if not exists fee_percent numeric,
  -- ค่าธรรมเนียมขั้นต่ำต่อคำสั่ง (บาท) เช่น โบรกหุ้นไทยหลายเจ้าคิดขั้นต่ำ 50 บาท/วัน
  add column if not exists fee_min_thb numeric;
