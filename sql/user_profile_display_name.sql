-- ชื่อที่ผู้ใช้ตั้งเอง สำหรับหน้า "ข้อมูลส่วนตัว"
--
-- เดิมชื่อบนหน้าโปรไฟล์ถูกบังคับมาจาก Google (user_metadata.full_name) และถ้าไม่มี
-- ก็ตัดหน้า @ ของอีเมลมาใช้ — แก้ไม่ได้เลยทั้งสองทาง ทั้งที่เป็นข้อมูลส่วนตัวที่สุด
--
-- ไม่เก็บใน auth.users.user_metadata เพราะนั่นเป็นของ Google ที่ถูกเขียนทับใหม่
-- ทุกครั้งที่ล็อกอิน — ชื่อที่ตั้งเองจะหายหลังเข้าระบบรอบถัดไป
--
-- เป็นคอลัมน์ทางเลือก: โค้ดฝั่ง saveUserProfile ตัดคอลัมน์นี้ทิ้งแล้วลองใหม่เอง
-- ถ้ายังไม่ได้รันไฟล์นี้ (แก้ชื่ออื่นได้ปกติ แค่ชื่อที่แสดงจะยังไม่ถูกบันทึก)
--
-- รันที่ Supabase SQL editor ครั้งเดียว (idempotent รันซ้ำได้)

alter table public.user_profile
  add column if not exists display_name text;
